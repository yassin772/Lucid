# Lucid local configuration placeholder.
# Never commit a real .env file.

LUCID_BACKEND_ROOT=
LUCID_PAYLOAD_PATH=api/lucid_payload.json

# Supabase public browser configuration.
# The anon key is public when Row Level Security is enabled.
SUPABASE_URL=
SUPABASE_ANON_KEY=
